import React from 'react';

/**
 * This component is obsolete as its functionality has been merged into `MyDashboard.tsx`.
 * It is kept empty to avoid breaking imports and will be removed in a future cleanup.
 */
const RiwayatPresensi: React.FC = () => {
  return null;
};

export default RiwayatPresensi;